function getSecureURL(store) {
  //This method is throwing a js error because there is no CloudCrazeUtils var or class.
}

function redirectSecure(store) {
  //This method was calling getSecureURL which did nothing other than throw an exception.
}
